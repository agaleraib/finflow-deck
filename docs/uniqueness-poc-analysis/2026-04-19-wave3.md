# Wave 3 — Structural Variants Validation

**Date:** 2026-04-19
**Spec:** docs/specs/2026-04-16-structural-variants.md (Phase 4)
**Wave plan:** docs/plan.md Wave 3
**Wave 2 baseline merge:** `b62db17`

**Runs:**
- fed-rate-decision: `uniqueness-poc-runs/2026-04-19T15-46-10-465Z_fed-rate-pause-2026-04-03/` ($0.73, 6.5 min)
- bitcoin-etf-approval: `uniqueness-poc-runs/2026-04-19T15-52-59-467Z_bitcoin-etf-approval-2026-03-15/` ($0.78, 6.8 min)

Both events ran with `--full --editorial-memory`, Postgres backend (`PostgresEditorialMemoryStore` at 10.1.10.230:5432), against the post-Wave-2 broker fixtures (a=v1, b=v2, c=v3, d=v1).

---

## Verdict

**ITERATE (with important revision — see Post-hoc consolidation section below).**

Initial read based on internal-run segmentation: variants produce visibly distinct structural shapes (sanity check passes), and bitcoin shows the spec §5.1-predicted cosine drop on different-variant pairs (−0.05, in the 0.03–0.08 estimate range). Fed-rate shows essentially no movement on the internal same-variant/different-variant segmentation, suggesting sample noise with n=1 same-variant pair. One pair regressed hard (premium↔fasttrade fid=0.75, `fabrication_risk`) — reasoned attribution to persona-prompt, not variant.

**Post-hoc revision:** direct A/B against the 2026-04-15 pre-variant run of the same event shows variants moved `distinct_products` from 2/6 to 5/6 (the production-gate metric), eliminated all `reskinned_same_article` pairs (2 → 0), and halved `fabrication_risk` (2 → 1). The original ITERATE verdict rested on the wrong baseline comparison. Corrected framing: **variants demonstrably move the gate on an apples-to-apples A/B.** Remaining blocker (fasttrade-pro persona) is an independent workstream, not a variant-layer defect. See Post-hoc consolidation section for full detail.

Strict ITERATE remains defensible (only 1 event with apples-to-apples A/B, code drift between baselines, Stage 6 tests only 1 identity), but the case for SHIP-the-variant-layer-with-known-persona-blocker is substantially stronger than this writeup's initial pass conveyed.

---

## Headline numbers

Stage 6 is `in-house-journalist × 4 personas` per event — 4 outputs and 6 pairs each, 12 pairs total across both events.

**Persona/variant assignment (constant across both events):** premium-capital-markets v1, fasttrade-pro v2, helix-markets v3, northbridge-wealth v1.

**Pair distribution per event:** 1 same-variant pair (premium↔northbridge, both v1) + 5 different-variant pairs.

| Event | Bucket | n | Mean cosine | Mean ROUGE-L | Mean fidelity | Min fidelity | Mean presentation |
|-------|--------|---|-------------|--------------|---------------|--------------|-------------------|
| fed-rate-decision | same-variant | 1 | 0.8563 | 0.197 | 0.92 | 0.92 | 0.58 |
| fed-rate-decision | different-variant | 5 | 0.8564 | 0.197 | 0.904 | **0.75** ⚠️ | 0.476 |
| bitcoin-etf-approval | same-variant | 1 | 0.9163 | 0.213 | 0.95 | 0.95 | 0.48 |
| bitcoin-etf-approval | different-variant | 5 | 0.8661 | 0.183 | 0.932 | 0.92 | 0.508 |

**Cosine delta (different − same):**
- fed-rate-decision: **+0.0001** (no movement)
- bitcoin-etf-approval: **−0.0502** (in spec §5.1 estimate 0.03–0.08)

**ROUGE-L delta (different − same):**
- fed-rate-decision: **+0.000** (no movement)
- bitcoin-etf-approval: **−0.030** (just below spec §5.1 estimate 0.08–0.15)

**Fidelity floor:**
- fed-rate-decision: mean 0.904 (right at floor); min **0.75 — fails ≥0.90 floor**
- bitcoin-etf-approval: mean 0.932; min 0.92 (above floor)

**Presentation similarity (lower = more visually different):**
- fed-rate: same 0.58 → diff 0.476 (drop of 0.10 — directionally correct, variants differentiate visually)
- bitcoin: same 0.48 → diff 0.508 (slight INCREASE — counterintuitive; pulled by helix↔northbridge `reskinned_same_article` with pres=0.62; n=1 control noise)

---

## Per-pair detail

### fed-rate-decision

| Pair | Variants | Cosine | ROUGE-L | Fidelity | Presentation | Verdict |
|------|----------|--------|---------|----------|--------------|---------|
| premium ↔ fasttrade | 1↔2 | 0.8514 | 0.183 | **0.75** ⚠️ | 0.52 | `fabrication_risk` |
| premium ↔ helix | 1↔3 | 0.8808 | 0.202 | 0.98 | 0.42 | distinct_products |
| premium ↔ northbridge | 1↔1 (SAME) | 0.8563 | 0.197 | 0.92 | 0.58 | distinct_products |
| fasttrade ↔ helix | 2↔3 | 0.8320 | 0.233 | 0.92 | 0.48 | distinct_products |
| fasttrade ↔ northbridge | 2↔1 | 0.8598 | 0.178 | 0.92 | 0.58 | distinct_products |
| helix ↔ northbridge | 3↔1 | 0.8581 | 0.189 | 0.95 | 0.38 | distinct_products |

Largest mover: fasttrade↔helix (cos 0.832 — lowest pair, both non-default variants).
Smallest mover: premium↔helix (cos 0.881 — v1↔v3, surprisingly close).

### bitcoin-etf-approval

| Pair | Variants | Cosine | ROUGE-L | Fidelity | Presentation | Verdict |
|------|----------|--------|---------|----------|--------------|---------|
| premium ↔ fasttrade | 1↔2 | 0.8892 | 0.199 | 0.92 | 0.58 | distinct_products |
| premium ↔ helix | 1↔3 | 0.8859 | 0.192 | 0.92 | 0.58 | distinct_products |
| premium ↔ northbridge | 1↔1 (SAME) | 0.9163 | 0.213 | 0.95 | 0.48 | distinct_products |
| fasttrade ↔ helix | 2↔3 | 0.8631 | 0.181 | 0.92 | 0.38 | distinct_products |
| fasttrade ↔ northbridge | 2↔1 | 0.8544 | 0.179 | 0.95 | 0.38 | distinct_products |
| helix ↔ northbridge | 3↔1 | 0.8380 | 0.164 | 0.95 | 0.62 | `reskinned_same_article` |

Largest mover: helix↔northbridge (cos 0.838 — lowest pair, BUT judge flagged as same article reskinned, presentation 0.62).
Same-variant pair stands out (cos 0.916) — clearest separation between same-variant and different-variant of the run.

---

## Visual sanity check (deferred from Wave 2)

In-house-journalist on fed-rate-decision, three personas with three different variants (first 320 chars verbatim from `crossTenantMatrix.outputs[]`):

**variant 1 — premium-capital-markets (Classic Column):**
```
# Dollar Squeeze Tightens: The Short EUR/USD Trade Is Working — and That Is Precisely the Problem

**LONDON, 10 April 2026** — The euro fell through 1.0820 on Wednesday and kept going. By the time Asian markets had finished their overnight session, EUR/USD had printed 1.0775, confirming what the Federal Reserve's hawki...
```

**variant 2 — fasttrade-pro (Inverted Pyramid with Sidebar):**
```
# Fed Kills the Rate-Cut Dream — Sell EUR/USD Into Every Bounce

**FASTTRADEPROTRADE DESK | APRIL 3, 2026**

The Federal Reserve just handed EUR/USD bears their second major catalyst in under a month. Wednesday's decision to hold rates at 5.25%–5.50% while slashing projected 2026 cuts from three to just one sent the pa...
```

**variant 3 — helix-markets (Market Dispatch):**
```
**The crowd is finally short the euro — and that's the part that should make bears nervous.**

**The Fed's message.** The Federal Reserve held rates at 5.25%–5.50% on April 3rd and delivered something the market had not fully priced: a dot plot that stripped out two of the three cuts previously pencilled in for 2026, l...
```

The three variants produce **dramatically different opening structures.** v1 has a traditional headline + dateline. v2 has a punchy headline + bold desk slug. v3 has NO `#` heading at all — it opens with the bold market-read sentence (the "headline IS the lead" pattern from spec §3.2 variant 3). This is the strongest evidence in the run that the variant prompts are driving real structural differentiation, even when cosine numbers move only modestly.

---

## Anomalies and caveats

### 1. Sample size: 1 same-variant pair per event

The persona-fixture distribution (a=v1, b=v2, c=v3, d=v1) gives only ONE same-variant pair per event (premium ↔ northbridge, both v1). With n=1, the same-variant baseline is essentially a single noisy data point. The cosine deltas above should be read as directional evidence, not confidence-intervalled measurements. To strengthen: either add more brokers (e.g. e=v2, f=v3) or add events that include cross-variant baseline pairs.

### 2. Fed-rate fidelity outlier (premium↔fasttrade, fid=0.75)

The judge flagged this pair `fabrication_risk` and gave it 0.75 fidelity. Inspecting `judgeFactualDivergences`:

- Document A (premium): "1.0650–1.0750 over the next two to four weeks" (range, no probability)
- Document B (fasttrade): "60% probability to a grind toward 1.0700" (point estimate + probability)
- Plus framing differences: A warns of crowded-trade short-squeeze risk; B presents the trade as accelerating with no countervailing caveat

These are forward-guidance / probability-assignment differences — not invented levels or fabricated data points (the underlying Fed numbers, rate spreads, and dot-plot facts are shared between both). The risk is **persona-driven** (fasttrade-pro's "salesman register" tone with confident probability assertions vs premium-capital-markets' measured advisory voice), not variant-driven (variant 2's structural template doesn't ask for probability assignments). Mitigation should target the persona prompt (constraining how fasttrade-pro frames forward views) rather than the variant.

### 3. broker-c v3 clamps to v1 on 2-variant identities

Spec §2.3 documented clamping: helix-markets (v3) on newsletter-editor or beginner-blogger would resolve to v1. Stage 6 only ran in-house-journalist (3-variant identity), so no clamping affected this wave. If future runs widen Stage 6 to multiple identities, expect the v3-clamp pairs to look identical to v1-baseline pairs and inflate the same-variant bucket artificially.

### 4. Editorial memory injection

Both runs logged `✓ injected editorial memory context for 4/4 personas` and `✓ recording 4 Stage 6 output(s) to editorial memory`. Backend confirmed Postgres on both events (`PostgresEditorialMemoryStore (postgresql://***@10.1.10.230:5432/finflow_dev)`). Editorial memory is exercising the production code path, not the in-memory fallback.

### 5. Stage 6 covers ONE identity, not all 6

The `crossTenantMatrix` in `raw-data.json` only contains in-house-journalist outputs. Per the runner, Stage 6 runs the load-bearing test on a single identity. If we want variant deltas across all 6 identities, that's a runner expansion (out of scope for this wave per spec §9 constraint on PoC scope, but worth flagging — the structural-variants test surface is currently 6× thinner than the persona-fixture distribution suggests).

### 6. fed-rate cross-tenant cosine essentially flat across buckets (0.8563 vs 0.8564)

Two interpretations: (a) variants don't help on macro-policy events because the topic compresses prose toward a common Fed/spread/dot-plot backbone regardless of structural template, OR (b) the n=1 same-variant pair happened to land at the diff-variant mean by coincidence. Cannot distinguish with this sample. Bitcoin's clean −0.05 delta argues for (a) being event-specific rather than the variant approach being fundamentally broken.

---

## Post-hoc consolidation (2026-04-19, no new LLM calls)

The original writeup's ITERATE verdict rested on a wrong comparison. I cited the memory line "2026-04-08 Stage 6 baseline: 5/6 distinct_products" as the pre-variant reference. That was for a **different event** (earlier PoC work, not fed-rate-pause) and at a different time in the prompt evolution. The correct apples-to-apples baseline is the 2026-04-15 run of the same event. Re-running the A/B against that baseline flips the conclusion significantly.

### A. Direct A/B: 2026-04-15 fed-rate-pause (variant OFF) vs Wave 3 r2 (variant ON)

Same event (`fed-rate-pause-2026-04-03`). Same 4 personas (premium-capital-markets, fasttrade-pro, helix-markets, northbridge-wealth). Same embedding model, same judge. Difference: structuralVariant `null` in 2026-04-15 (pre-Wave-1) vs `1/2/3/1` in Wave 3 r2. Historical run at commit `028bdd8`; Wave 3 at post-`b62db17`. Code drift between those commits is a confounder — angle/personality prompts, conformance changes, any runner tweaks could contribute to the delta. Read with that caveat.

| Metric | 2026-04-15 (OFF) | Wave 3 r2 (ON) | Δ | Spec §5.1 estimate | In range? |
|--------|------------------|-----------------|---|--------------------|-----------|
| Mean cosine | 0.9000 | 0.8564 | **−0.044** | 0.03–0.08 drop | ✅ |
| Mean ROUGE-L | 0.2247 | 0.1969 | −0.028 | 0.08–0.15 drop | ❌ below estimate |
| Mean judge presentation | 0.577 | 0.493 | **−0.084** | (directional match) | — |
| Mean judge fidelity | 0.905 | 0.907 | +0.002 | unchanged | ✅ |
| `distinct_products` | 2/6 (33%) | 5/6 (83%) | **+50pp** | — | ✅ strong gain |
| `reskinned_same_article` | **2/6 (33%)** | **0/6 (0%)** | **−33pp** | — | ✅ eliminated |
| `fabrication_risk` | 2/6 (33%) | 1/6 (17%) | **−17pp** | — | ✅ reduced |

The 50pp jump in `distinct_products` (the production-gate metric) is the headline. The 2026-04-15 baseline had only 2 of 6 cross-tenant pairs passing the judge; Wave 3 r2 has 5 of 6. The `reskinned_same_article` verdict — the exact failure mode variants were designed to prevent — dropped from 2 pairs to zero. Cosine and presentation similarity moved directionally in the expected direction with magnitudes in or near the spec §5.1 estimate range.

**Variants demonstrably move the production-gate metric on an apples-to-apples A/B.** The code drift caveat means we can't attribute 100% of the delta to variants alone (angle/personality tag tuning between 2026-04-15 and 2026-04-19 could contribute), but the direction and magnitude strongly support the variant layer carrying material weight.

### E. Persona attribution for fasttrade fabrication — now measured, not reasoned

| Event | Fasttrade variant | Pair | Fidelity | Judge verdict |
|-------|-------------------|------|----------|---------------|
| 2026-04-15 fed-rate (baseline, variant OFF = default v1) | 1 (default) | premium ↔ fasttrade | 0.75 | `fabrication_risk` |
| 2026-04-15 fed-rate | 1 (default) | fasttrade ↔ helix | 0.88 | `fabrication_risk` |
| Wave 3 r2 fed-rate (variants ON) | 2 | premium ↔ fasttrade | 0.75 | `fabrication_risk` |
| Wave 3 r2 fed-rate | 2 | fasttrade ↔ helix | 0.92 | `distinct_products` |

Same persona (`fasttrade-pro`), two different structural variants (default-1 in baseline, 2 in Wave 3), identical fabrication behavior on the premium-pair (fid=0.75 both runs). The variant is demonstrably not the cause of the fabrication risk. The fasttrade-pro persona prompt is. This is now a **measured** attribution, not a reasoned one.

Stage 5 and Stage 6 outputs of fasttrade-pro in Wave 3 both carry probability-assertion language ("60% probability," explicit point-estimate predictions) that does not appear in the source FA. Same pattern present in the 2026-04-15 baseline fasttrade output. The persona's "salesman register" voice is baked into the persona prompt itself, independent of variant selection.

### G. Stage 3.5 identity-format-diversity verdict breakdown (consistency check)

Stage 3.5 compares the 6 identity agents pairwise with no persona overlay — it measures identity-format diversity, not brand coherence (see `project_uniqueness_poc_stage_semantics.md` for the disambiguation; the user-facing label was renamed in commit c9e8071 to reflect this).

| Run | distinct | reskinned | fabrication | pair count | % problematic |
|-----|----------|-----------|-------------|------------|---------------|
| 2026-04-15 fed-rate (OFF) | 5 | 2 | 1 | 8 | 37.5% |
| Wave 3 r2 fed-rate (ON) | 9 | 2 | 1 | 12 | 25.0% |
| Wave 3 bitcoin (ON) | 7 | 0 | 3 | 10 | 30.0% |

Roughly stable rate. Variants don't apply to Stage 3.5 (no persona overlay = no variant selection), so this is expected. The identity-format diversity layer is unchanged across the A/B, supporting the conclusion that the cross-tenant gate improvement in A is coming from the variant + persona layer, not from any change in the underlying identity templates.

### F. Editorial memory state

2026-04-15 historical manifest shows `"editorialMemoryState": null` — the postgres store was empty at run start. Wave 3 r2 was the first successful run of the session (r1 died before writing Stage 6 outputs to memory). Both runs started from effectively empty editorial memory — so the memory-injection log lines mean minimal injected context in both cases. Editorial memory is not a confound between baseline and treatment; the comparison is clean on that axis.

### Revised verdict

The A/B against the correct baseline inverts the framing significantly:

- Original verdict (ITERATE, cited 5/6 baseline from wrong event): variants didn't measurably move the gate.
- Corrected verdict (against 2026-04-15 same-event baseline): **variants moved distinct_products from 2/6 to 5/6 on the production-gate metric, eliminated all reskinned_same_article pairs, and halved fabrication_risk** — which, together with the E finding that the remaining fabrication is persona-prompt-driven not variant-driven, means variants are working as designed.

Strict ITERATE remains defensible because:
- Only one event has an apples-to-apples A/B (fed-rate-pause). Bitcoin lacks a matching historical baseline
- Code drift between 2026-04-15 (`028bdd8`) and Wave 3 (post-`b62db17`) is a confounder
- Stage 6 still tests only in-house-journalist
- n=2 events total

But the case for **SHIP-the-variant-layer** (with fasttrade persona fix as an independent workstream) is substantially stronger than the original writeup indicated. Production-pipeline wiring is not gated on fasttrade — they are separable concerns. The corrected framing: variants are a measurable improvement on the load-bearing metric; the persona layer has an independent defect that the PoC surfaces regardless of variant rollout.

---

## Next steps (drives the verdict)

**Variant approach is alive but underdetermined.** Recommendations in priority order:

1. **Grow the same-variant control set.** Add 2-3 more brokers to fixtures with variant 1, OR rotate through {v1, v2, v3, v1} → {v1, v2, v3, v2} on alternate runs to get same-variant pairs at v2 and v3 baselines too. Goal: get same-variant n ≥ 5 per event so deltas have meaningful confidence intervals.

2. **Investigate fasttrade-pro persona prompt.** The fed-rate fid=0.75 outlier on premium↔fasttrade attributes to forward-guidance framing — a persona-prompt issue, not a variant-prompt issue. Either constrain fasttrade-pro's probability-assertion behavior, or accept that this persona will produce more divergent forward views on macro events. This is independent of structural variants and should be triaged separately before re-running validation.

3. **Re-run on 3-4 events** spanning topic diversity (one rate event, one credit event, one geopolitical event, one crypto event). Two events is the spec floor, not the comfort zone.

4. **Widen Stage 6 to ≥3 identities.** Currently it tests in-house-journalist only — that's one slice of the variant surface. Adding senior-strategist (3 variants, distinct templates) and educator (3 variants, distinct templates) would 3× the test surface for the same persona budget.

5. **Variant 3 specifically looks promising** based on the visual sanity check — it produces the most structurally distinct output (no `#` heading, bold-sentence opener, market-dispatch format). If anything is shippable today, it's that the variant prompts themselves work; the validation surface (1 identity × 4 personas) is too narrow to confirm.

**Do NOT open production-pipeline wiring yet.** Wave 3 verdict is ITERATE, not SHIP — production wiring is gated on a SHIP verdict per the synthetic spec and spec §9 constraint.
